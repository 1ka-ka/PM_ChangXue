# 《功能需求详述》（《"畅学"社区 PRD V1.0》附录）

> 本文档为 PRD 总册 V1.0 的附录，随开发迭代持续细化完善。总册定义功能范围、优先级与全局业务规则；本文档定义每个功能的操作流程、交互规则、异常场景与验收标准。全局规则（积分数值、采纳规则、状态机等）以总册第 6 章为准，本文档不重复。
> 功能编号与总册第 5 章功能列表一一对应。

| 项目 | 内容 |
| --- | --- |
| 创建日期 | 2026-09-01 |
| 版本号 | V1.0（与 PRD 总册同步） |
| 修订记录 | 见 PRD 总册 1.1（与总册同步修订） |

**详述状态说明**：P0 功能已完整详述（直接指导 MVP 开发）；P1 功能按已确认规则部分详述，缺失处以 `[待补充]` 标记，于 V1.x 开发前补齐；P2 功能为占位区块，于 V2.0 开发前补齐。

---

## 第一部分：P0 功能（MVP 交付，完整详述）

### 功能模块：M1 账号与个人中心

#### 功能名称：注册/登录【M1-F1｜P0】

- **功能描述**：用户创建账号并登录社区，获得发帖、回答等全部功能权限，注册即赠送 50 积分。
- **用户操作流程**：
    1. 用户进入社区，在登录页点击"注册"。
    2. 按[待补充：登录方式为前置决策项——账号密码/手机验证码/第三方]完成账号创建。
    3. 设置昵称并选择头像（不设置则使用系统默认头像）。
    4. 注册成功，系统赠送 50 积分并提示，自动登录进入广场首页。
    5. 老用户在登录页输入账号信息，点击"登录"，校验通过后进入广场首页。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：用户处于未登录状态；访问发帖、回答、点赞等需登录功能时自动跳转登录页。
    - **核心处理逻辑**：注册成功创建用户记录与积分账户，写入积分流水"注册赠送 +50"（规则见总册 §6.3）；登录态有效期[待补充]；是否允许多端同时在线[待补充]；昵称经内置敏感词库校验，含敏感词则拒绝并提示。
    - **界面交互规则**：注册/登录按钮点击后进入加载态并禁用，防止重复提交；字段校验失败时行内实时提示；注册成功后 Toast 提示"欢迎加入畅学，已赠送 50 积分"，随后跳转广场首页。
- **异常场景处理**：
    - 账号已存在（注册）：提示"该账号已注册，请直接登录"，并提供跳转登录入口。
    - 凭证错误（登录）：提示"账号或密码/验证码错误"，连续失败次数上限与锁定策略[待补充]。
    - 验证码错误或过期：提示"验证码错误或已过期，请重新获取"（若采用验证码方式）。
    - 网络超时：提示"网络异常，请重试"，已填内容保留不清空。
- **验收标准**：
    1. Given 新用户，When 完成注册流程，Then 积分账户余额为 50，且存在"注册赠送 +50"流水记录。
    2. Given 已注册账号，When 使用错误凭证登录，Then 提示错误且不产生登录态。
    3. Given 未登录用户，When 点击"发布提问"，Then 跳转登录页，登录成功后返回发帖页。
    4. Given 昵称包含敏感词，When 提交注册，Then 提示"昵称含违禁内容，请修改"且注册不完成。

#### 功能名称：个人资料设置【M1-F2｜P0】

- **功能描述**：用户编辑昵称、头像、学校、专业四项资料（全部自填，无认证）。
- **用户操作流程**：
    1. 用户进入"我的 → 编辑资料"页。
    2. 修改昵称、上传头像、填写学校与专业（自由文本或预设列表[待补充：学校库来源]）。
    3. 点击"保存"，提示保存成功。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：用户已登录；昵称长度 1-20 字符[待补充：最终字数上限]。
    - **核心处理逻辑**：学校/专业写入用户属性，作为 P1"本校/本专业过滤"（M1-F4）的数据来源；昵称修改频率限制[待补充]；昵称经敏感词库校验。
    - **界面交互规则**：头像上传后即时预览裁剪；保存按钮点击后加载态，成功 Toast"保存成功"。
- **异常场景处理**：
    - 头像上传失败：提示"头像上传失败，请重试"，保留原头像。
    - 昵称为空或超长：行内提示，禁止保存。
    - 网络异常：提示重试，表单内容保留。
- **验收标准**：
    1. Given 已登录用户，When 修改学校为"XX 大学"并保存，Then 个人主页展示更新为"XX 大学"。
    2. Given 昵称为空，When 点击保存，Then 提示"请填写昵称"且不保存。

#### 功能名称：个人主页【M1-F3｜P0】

- **功能描述**：展示用户公开信息（资料、感谢值）、积分（本人视角），及其发布的帖子、回答与收藏列表，并提供周助人榜入口。
- **用户操作流程**：
    1. 用户点击底部/侧边"我的"进入个人主页。
    2. 查看个人信息卡：昵称、头像、学校、专业、感谢值（本周/本月/累计）、积分余额与积分明细入口（本人视角）。
    3. 切换内容 Tab：TA 的帖子 / TA 的回答 / 收藏，浏览对应列表。
    4. 点击周助人榜入口，查看本周榜单（M5-F19）。
    5. 点击任意列表项进入帖子详情。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；浏览他人主页时无积分与积分明细入口（积分对他人是否可见[待补充：建议仅本人可见]）。
    - **核心处理逻辑**：帖子列表为该用户发布的全部帖子（含待解决/已解决）；回答列表为该用户全部回答（含被采纳标记）；收藏列表仅本人可见[待补充：待评审确认默认值]；积分明细页展示全量流水（来源/变动值/余额/时间，见 M5-F17）。
    - **界面交互规则**：Tab 切换不整页刷新；列表分页加载，触底加载下一页。
- **异常场景处理**：
    - 列表为空：各 Tab 显示对应空态插画与文案（如"还没有发布过提问，去提问吧"）。
    - 加载失败：显示重试按钮。
- **验收标准**：
    1. Given 用户被采纳 1 次，When 查看个人主页，Then 感谢值累计显示 +30，且本周/本月统计同步。
    2. Given 用户收藏了一个帖子，When 查看收藏 Tab，Then 该帖在列；取消收藏后不再在列。
    3. Given 本人视角，When 查看主页，Then 可见积分余额并可进入积分明细；他人视角不可见[待评审确认]。

---

### 功能模块：M2 问答核心

#### 功能名称：发帖提问（含悬赏）【M2-F6｜P0】

- **功能描述**：用户以文字和图片形式发布带标题与领域标签的问题帖，可选设置积分悬赏提升推荐曝光。
- **用户操作流程**：
    1. 用户在广场页点击发布按钮，进入发帖编辑器。
    2. 输入正文，插入 1 张或多张图片（如题目照片）。
    3. 填写标题（必填），选择 1 个及以上领域标签（必填，标签集见总册待定项 3）。
    4. 可选：设置悬赏档位（10/20/50/100 积分）。
    5. 查看 AI 摘要预览区：P0 显示占位文案"AI 摘要功能即将上线"（M8-F32 为 P1）。
    6. 点击"发布"，校验通过后跳转帖子详情页。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；正文非空或至少含 1 张图片；标题必填（1-50 字[待补充]）；标签 1-3 个[待补充：上限]；图片数量与大小上限[待补充]。
    - **核心处理逻辑**：悬赏校验积分余额，不足则阻断并提示（悬赏为发布时一次性扣除，仅购买推荐加权，不退回不分配给回答者，见总册 §6.3）；发布成功帖子状态置"待解决"，进入广场最新流；悬赏帖写入推荐加权标记（M3-F13）并记录积分流水"悬赏 -N"；编辑器正文本地缓存，异常退出不丢失[待补充：完整草稿箱范围]。
    - **界面交互规则**：发布按钮点击后加载态并禁用；必填项缺失时对应字段高亮并提示"请填写标题/请选择标签"；悬赏档位为四档单选，默认不悬赏；发布成功 Toast 并跳转详情页。
- **异常场景处理**：
    - 悬赏积分不足：弹窗提示"积分不足，可调整悬赏档位或发布普通帖"，提供"调整悬赏"与"普通发布"两个选项。
    - 图片上传失败：该图片位显示重试按钮，不影响其他图片与正文。
    - 发布请求超时：提示"发布失败，请重试"，编辑器内容保留。
    - 含敏感词：提示"内容含违禁内容，请修改后发布"，不指明具体词。
- **验收标准**：
    1. Given 未填写标题，When 点击发布，Then 发布失败且标题字段高亮提示"请填写标题"。
    2. Given 积分余额 8，When 选择 10 积分悬赏并发布，Then 弹窗提示积分不足，帖子未发布，余额不变。
    3. Given 合法内容 + 50 积分悬赏，When 发布成功，Then 积分余额立减 50，流水记录"悬赏 -50"，帖子带悬赏标识。
    4. Given 发布成功，When 任意用户在广场查看该帖，Then 帖子显示"待解决"状态与发布时间。
    5. Given 仅上传 1 张清晰图片、正文为空，When 发布，Then 发布成功。

#### 功能名称：回答【M2-F7｜P0】

- **功能描述**：用户对帖子提交正式回答；已解决帖仍可被继续回答。
- **用户操作流程**：
    1. 用户在帖子详情页点击"写回答"，进入回答编辑器。
    2. 输入回答内容（文字[待补充：P0 回答是否支持图片——建议支持，理科解题需要过程图]）。
    3. 点击"提交"，回答出现在回答列表。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；不能回答自己的帖子[待补充：是否允许自问自答，建议不允许，待评审]；内容非空。
    - **核心处理逻辑**：每用户每帖限 1 条回答，再次进入编辑器加载已有回答供编辑[待补充：待评审确认]；回答提交成功后通知提问者（M6-F24）；已解决帖接受新回答并正常展示。
    - **界面交互规则**：提交按钮加载态防重；提交成功编辑器关闭，列表插入新回答并高亮。
- **异常场景处理**：
    - 未登录点击"写回答"：跳转登录页，登录后返回详情页。
    - 内容为空：提示"回答内容不能为空"。
    - 网络异常：提示重试，内容保留。
- **验收标准**：
    1. Given 已解决的帖子，When 用户提交新回答，Then 回答成功展示且提问者收到通知。
    2. Given 未登录用户，When 点击"写回答"，Then 跳转登录页。
    3. Given 已提交过回答的用户，When 再次点击"写回答"，Then 加载已有回答进入编辑态[待评审确认]。

#### 功能名称：采纳回答【M2-F8｜P0】

- **功能描述**：提问者采纳帮助了自己的回答（最多 3 个），多采纳时指定 1 个"最佳答案"，可随时更换最佳。
- **用户操作流程**：
    1. 提问者在帖子详情页的目标回答上点击"采纳"。
    2. 二次确认弹窗："确认采纳该回答？采纳后帖子将标记为已解决。"
    3. 确认后该回答显示"采纳答案"标签；被采纳者收到通知。
    4. 存在多个采纳时，提问者点击某一已采纳回答上的"设为最佳"，该回答显示"最佳答案"并置顶。
    5. 可点击其他已采纳回答"设为最佳"完成更换。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：仅提问者本人可见并可操作"采纳"按钮；每帖采纳上限 3 个（规则见总册 §6.2）。
    - **核心处理逻辑**：首次采纳触发帖子状态"待解决→已解决"并自动收录知识库（M4-F15）；每次采纳向回答者发放 +30 积分与 +30 感谢值（固定分值，更换最佳不追回不补发）；追加采纳同步更新知识库内容；取消采纳[待补充：是否允许及回退逻辑，待评审]。
    - **界面交互规则**：采纳确认弹窗防止误触；被采纳回答即时显示标签；多采纳时最佳答案固定置顶；帖子头部状态标识即时更新为"已解决"。
- **异常场景处理**：
    - 已达 3 个采纳再采纳：提示"最多可采纳 3 个回答"。
    - 采纳请求失败：提示重试，状态不中途变更。
- **验收标准**：
    1. Given 提问者首次采纳某回答，When 操作成功，Then 帖子状态为"已解决"，回答者积分 +30、感谢值 +30，帖子进入知识库。
    2. Given 已采纳 3 个回答，When 采纳第 4 个，Then 提示"最多可采纳 3 个回答"且操作被拒绝。
    3. Given 已有 2 个采纳，When 将其中之一设为最佳，Then 该回答显示"最佳答案"并置顶，另一个显示"采纳答案"。
    4. Given 更换最佳答案，When 操作成功，Then 双方积分与感谢值均无变动。
    5. Given 非提问者用户，When 查看回答列表，Then 不可见"采纳"按钮。
    6. Given 已解决帖，When 提问者追加采纳新回答，Then 新回答者获得 +30/+30，知识库中该帖的采纳答案列表同步增加。

#### 功能名称：双层评论【M2-F9｜P0】

- **功能描述**：用户可对帖子与回答发表评论，评论可被回复（二层封顶）与点赞。
- **用户操作流程**：
    1. 用户在帖子详情页（对帖子）或某条回答下方（对回答）的评论输入框输入内容，点击"发送"。
    2. 评论出现在对应评论区第一层。
    3. 点击某条评论的"回复"，输入框带 @对象 前缀，发送后该回复嵌套展示于该评论下（第二层）。
    4. 点击评论的"点赞"图标进行点赞/取消。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；评论对象为帖子或回答；回复对象为第一层评论。
    - **核心处理逻辑**：回复的回复不再产生第三层，归并为对同一第一层评论的回复并以 @被回复人 展示（二层封顶）；回复成功通知被回复人，评论成功通知内容作者（帖子作者/回答者）；删除评论时级联删除其全部回复；评论不可被收藏。
    - **界面交互规则**：输入框为空时"发送"按钮置灰；发送后即时插入列表；二层回复默认按时间正序排列，超过[待补充：条数，如 5 条]可展开。
- **异常场景处理**：
    - 内容为空：发送按钮禁用。
    - 含敏感词：提示修改。
    - 被回复的评论已被删除：回复提交时提示"该评论已删除"。
- **验收标准**：
    1. Given 评论 A 存在，When 用户回复 A，Then 回复嵌套于 A 下方且 A 的作者收到通知。
    2. Given 评论 A 下已有 2 条回复，When 删除 A，Then 2 条回复同步不可见。
    3. Given 任一评论，When 用户尝试对其回复再回复（第三层），Then 系统归并为对第一层评论的回复并 @被回复人。
    4. Given 评论区，When 查看评论操作项，Then 仅有"回复/点赞"，无"收藏"。

#### 功能名称：点赞与收藏【M2-F10｜P0】

- **功能描述**：用户可对帖子和回答进行点赞与收藏，对评论仅可点赞。
- **用户操作流程**：
    1. 用户在帖子详情页或列表页点击帖子/回答的"点赞"图标，计数 +1；再次点击取消，计数 -1。
    2. 点击帖子/回答的"收藏"图标，图标变为已收藏态；再次点击取消收藏。
    3. 收藏的内容出现在个人主页"收藏"Tab（M1-F3）。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录。
    - **核心处理逻辑**：点赞幂等（重复请求不重复计数）；点赞产生通知（聚合策略[待补充：是否聚合防骚扰]）；收藏可取消，仅本人可见[待评审]；回答的点赞数参与回答排序（帖主采纳置顶优先，其次点赞数倒序）。
    - **界面交互规则**：点赞/收藏图标点击即时切换状态与计数；未登录点击时跳转登录页。
- **异常场景处理**：
    - 网络异常：图标回弹至原状态并提示"操作失败，请重试"。
    - 对象已被删除：提示"内容已删除"。
- **验收标准**：
    1. Given 未点赞的回答，When 连续快速点击点赞两次，Then 计数净变化为 0（点赞后取消），无重复计数。
    2. Given 用户收藏帖子 X，When 进入个人主页收藏 Tab，Then X 在列；取消收藏后刷新，X 不在列。
    3. Given 一条回答有 5 个赞，When 其所在帖有 2 个采纳回答，Then 列表排序为：最佳答案置顶、第二采纳次之、该 5 赞回答随其后（帖主选择优先于点赞数）。

---

### 功能模块：M3 广场与内容分发

#### 功能名称：帖子广场【M3-F12｜P0】

- **功能描述**：首页帖子信息流，展示全站全部帖子，通过 Tab 切换浏览维度。
- **用户操作流程**：
    1. 用户进入首页（广场），默认展示"推荐"Tab。
    2. 切换 Tab：推荐 / 最新 / 待解决。
    3. 上下滑动浏览帖子卡片（标题、摘要区占位、标签、状态、悬赏标识、发布时间、回答/点赞数）。
    4. 点击卡片进入帖子详情页（M2-F6 详情）。
    5. 触底自动加载下一页。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：支持未登录浏览；登录后可互动。
    - **核心处理逻辑**："最新"= 发布时间倒序；"待解决"= 无采纳帖按最后活跃时间倒序[待补充：最终排序键]；"推荐"= 基础热度（浏览/回答/点赞聚合）+ 悬赏加权（M3-F13）- 长期无采纳衰减（总册 §6.1），公式[待补充：评审确定]；P1 增加"本校/本专业"Tab（M1-F4）。
    - **界面交互规则**：Tab 切换保留各自浏览位置；下拉刷新重置列表；每帖卡片显示状态标识（待解决/已解决）与发布时间。
- **异常场景处理**：
    - 加载失败：页脚显示"加载失败，点击重试"。
    - 无更多内容：显示"已经到底啦"。
- **验收标准**：
    1. Given 同等热度的悬赏帖与非悬赏帖，When 查看推荐 Tab，Then 悬赏帖排序靠前。
    2. Given 发布超过 14 天（默认阈值）无采纳的帖子，When 查看推荐 Tab，Then 其排序显著低于近期活跃帖。
    3. Given 待解决 Tab，When 浏览列表，Then 列表中全部帖子均为"待解决"状态。
    4. Given 未登录用户，When 浏览广场，Then 可正常浏览但点击互动时跳转登录。

---

### 功能模块：M4 搜索与知识库

#### 功能名称：搜索【M4-F14｜P0】

- **功能描述**：用户通过标签筛选与标题关键词模糊匹配检索内容；知识库（已解决帖）优先展示，无结果时降级检索广场。
- **用户操作流程**：
    1. 用户点击广场搜索入口进入搜索页。
    2. 输入关键词（标题模糊匹配）和/或选择标签筛选。
    3. 提交搜索，结果列表优先展示知识库命中结果（带"已解决"标识）。
    4. 知识库无结果时，页面提示"知识库暂无相关已解决问题，为您展示广场相关帖子"，下方展示广场命中帖（带状态标识）。
    5. 两级均无结果时，展示空态页与"发布提问"引导按钮，点击进入发帖页。
    6. 点击任一结果进入帖子详情。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：支持未登录搜索。
    - **核心处理逻辑**：匹配方式为标题关键词包含匹配[待补充：分词方案] + 标签精确筛选；知识库结果=已收录帖（M4-F15）；降级逻辑触发时记录 `search_degrade` 埋点（验证"提问前查重"路径效果的关键事件）；结果排序[待补充：建议相关度优先，简化实现可为发布时间倒序]。
    - **界面交互规则**：搜索历史[待补充：P0 是否本地保留]；结果项区分"知识库/广场"来源标识；降级提示为横幅文案，清晰告知当前展示的是广场内容。
- **异常场景处理**：
    - 关键词为空且未选标签：搜索按钮禁用。
    - 请求超时：提示"搜索失败，请重试"。
- **验收标准**：
    1. Given 知识库存在标题含关键词的已解决帖，When 搜索该关键词，Then 结果优先且仅含知识库命中（带"已解决"标识），广场同关键词帖不混入。
    2. Given 知识库无命中、广场有命中，When 搜索，Then 显示降级提示横幅 + 广场结果。
    3. Given 两级均无命中，When 搜索，Then 空态页展示"发布提问"引导按钮且按钮可跳转发帖页。
    4. Given 降级发生，When 查看埋点，Then `search_degrade` 事件携带关键词与广场结果数上报。

#### 功能名称：知识库收录【M4-F15｜P0 系统功能】

- **功能描述**：帖子首次被采纳时自动收录知识库，收录"帖子 + 全部采纳答案"，供搜索优先消费。
- **用户操作流程**：（系统自动，无用户操作）用户在搜索页命中知识库内容即完成消费；帖子详情页被收录后显示"已解决"标识。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：帖子发生首次采纳（M2-F8）。
    - **核心处理逻辑**：收录内容 = 帖子（标题/标签/正文/图片）+ 全部采纳答案；后续追加采纳同步更新收录内容；未采纳帖永不入库（总册 §6.5）；答案展示顺序 = 帖主选择（最佳置顶）+ 点赞数；P0 无独立知识库浏览页，消费入口为搜索；独立浏览页[待补充：建议 P1]。
    - **界面交互规则**：搜索结果中知识库条目带"已解决"标识，与广场结果视觉区分。
- **异常场景处理**：
    - 收录写入失败：重试机制，不影响帖子本身的"已解决"状态；告警记录。
- **验收标准**：
    1. Given 帖子首次被采纳，When 用户以其标题关键词搜索，Then 该帖出现在知识库优先结果中。
    2. Given 未采纳帖，When 用户搜索其标题关键词，Then 该帖不出现在知识库结果，仅可能出现在降级广场结果。
    3. Given 已入库帖追加第 2 个采纳，When 搜索命中查看详情，Then 知识库内容包含 2 个采纳答案且顺序为最佳在前。

---

### 功能模块：M5 激励体系

#### 功能名称：积分账户与流水【M5-F17｜P0 系统功能】

- **功能描述**：记录每个用户的积分余额与全量变动流水，执行产出封顶与余额校验。
- **用户操作流程**：
    1. 用户在个人主页查看积分余额。
    2. 点击余额进入积分明细页，按时间倒序查看流水（来源、变动值、变动后余额、时间）。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：注册即创建积分账户（+50）。
    - **核心处理逻辑**：全部数值规则与封顶见总册 §6.3；日产出封顶 100（登录+任务+采纳合计，悬赏与商城消耗不计入封顶）；余额不可为负，任何扣除前先校验余额；每次变动写入流水（来源枚举：注册/登录/任务/采纳/悬赏/追回/商城）。
    - **界面交互规则**：明细页按日期分组展示；收入绿色+、支出红色-（具体视觉[待补充：设计稿确定]）。
- **异常场景处理**：
    - 并发扣除导致余额不足：以流水事务保证不透支，后到的扣除失败并回滚对应操作（如悬赏发布失败）。
- **验收标准**：
    1. Given 用户当日已通过登录与采纳累计产出 100 积分，When 第 2 次采纳发生，Then 该次采纳积分不发放（封顶），感谢值正常发放，且封顶事件有流水备注。
    2. Given 任何积分变动，When 查看明细页，Then 均存在对应流水且变动后余额连续正确。
    3. Given 余额 8 的用户，When 尝试 10 积分悬赏，Then 扣除失败、帖子不发布、余额不变。

#### 功能名称：助人榜（周/月）【M5-F19｜P0】

- **功能描述**：按本周/本月感谢值展示排名靠前的助人用户，呈现于主页与榜单页。
- **用户操作流程**：
    1. 用户在广场主页查看本周助人榜入口（Top 展示[待补充：Top N，建议 10]）。
    2. 点击"查看完整榜单"进入榜单页，切换周榜/月榜。
    3. 榜单项展示：排名、头像、昵称、感谢值。
    4. 点击榜单用户进入其个人主页。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：感谢值数据来自被采纳记录（M5-F18）。
    - **核心处理逻辑**：周榜每周一 0 点结算上周数据，月榜每月 1 日 0 点结算上月数据（总册 §6.4）；结算后榜单固定展示上周/上月结果直至下期结算；上榜门槛[待补充：P1 前的防刷门槛，待评审]。
    - **界面交互规则**：Top 3 特殊样式[待补充：设计稿]；榜单页注明结算周期（如"8 月 25 日 - 8 月 31 日"）。
- **异常场景处理**：
    - 结算任务失败：自动重试 + 告警，展示上一期数据并标注"结算更新中"。
- **验收标准**：
    1. Given 用户本周被采纳 3 次，When 查看本周榜单，Then 该用户感谢值 +90 实时计入排名。
    2. Given 周一 0 点结算完成，When 查看周榜，Then 榜单为上周数据且周期标注正确。
    3. Given 榜单用户，When 点击其榜单项，Then 跳转该用户个人主页。

---

### 功能模块：M6 通知中心

#### 功能名称：站内通知【M6-F24｜P0】

- **功能描述**：聚合展示用户收到的互动通知（被回答/被评论/被回复/被采纳/被点赞），点击直达对应内容。
- **用户操作流程**：
    1. 用户点击导航栏铃铛图标进入通知中心。
    2. 按时间倒序浏览通知列表，未读项有标识，铃铛显示未读计数。
    3. 点击某条通知，跳转对应内容（帖子/回答/评论位置）并标记该条已读。
    4. 可一键"全部已读"。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；触发源：我的帖子被回答、我的帖子/回答被评论、我的评论被回复、我的回答被采纳、我的内容被点赞。
    - **核心处理逻辑**：P0 为拉取式聚合列表，不做实时推送与离线推送[待补充：推送渠道为 P1 决策]；点赞通知聚合策略[待补充：同一对象聚合为一条"X 人赞了你的回答"]；被采纳通知内容含积分与感谢值变动提示。
    - **界面交互规则**：未读计数上限显示"99+"；点击通知后返回通知中心保持已读状态。
- **异常场景处理**：
    - 通知对应内容已删除：点击后提示"内容不存在或已删除"。
    - 加载失败：重试按钮。
- **验收标准**：
    1. Given 用户 A 的帖子被用户 B 回答，When A 打开通知中心，Then 存在"B 回答了你的问题"通知，点击直达该回答。
    2. Given 用户 A 的回答被采纳，When A 查看通知，Then 通知内容包含采纳与积分/感谢值增加提示。
    3. Given 存在 3 条未读通知，When 点击"全部已读"，Then 未读计数清零。

---

### 功能模块：M7 治理与合规

#### 功能名称：举报入口【M7-F25｜P0】

- **功能描述**：用户可对违规的帖子、回答、评论提交举报，进入管理员人工处理队列。
- **用户操作流程**：
    1. 用户在帖子/回答/评论的操作菜单中点击"举报"。
    2. 选择举报类型：垃圾广告 / 人身攻击 / 色情低俗 / 违法违规 / 其他[待补充：类型清单终版]。
    3. 可选填写补充说明（200 字内[待补充]）。
    4. 提交，提示"已收到举报，我们将尽快处理"。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：已登录；举报对象未被删除。
    - **核心处理逻辑**：P0 举报直接进入管理员人工队列（AI 分级为 P1，M7-F27）；同一用户对同一对象重复举报去重（仅记录首次）；被举报内容在处理前正常展示（P0 不做自动处理）。
    - **界面交互规则**：提交按钮加载态；提交成功后该对象操作菜单中"举报"变为"已举报"。
- **异常场景处理**：
    - 网络失败：提示重试，已填说明保留。
    - 对象已删除：提示"内容已删除，无需举报"。
- **验收标准**：
    1. Given 任意帖子，When 用户提交举报，Then 提示成功且管理员举报队列出现该记录（含对象、类型、说明、举报人）。
    2. Given 用户已举报对象 X，When 再次举报 X，Then 提示"你已举报过该内容"，队列不新增重复记录。

#### 功能名称：管理员基础处置【M7-F26｜P0】

- **功能描述**：管理员查看举报队列与内容详情，执行删除内容、封禁账号、追回积分等处置，全部操作留痕。
- **用户操作流程**：
    1. 管理员登录管理后台（独立端[待补充：P0 形态——独立 Web 页面]）。
    2. 查看举报队列列表（按时间倒序，含对象类型、举报类型、举报次数）。
    3. 点击进入处置页：查看被举报内容全文、作者信息、历史举报记录。
    4. 执行处置：删除内容 / 封禁账号 / 追回积分[待补充：封禁时长档位，如 1 天/7 天/永久]。
    5. 或判定"不违规"驳回举报。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：管理员账号为独立运营角色（普通用户无后台入口）；P0 管理员账号由超管在数据库/配置层创建[待补充：P0 账号创建方式]。
    - **核心处理逻辑**：删除内容 = 帖子/回答/评论不可见（知识库内容同步移除）；封禁账号 = 目标账号无法登录且内容处置策略[待补充：仅封号还是同时隐藏内容]；追回积分 = 扣减指定分数并写入"追回"流水，可指定单笔回收；全部处置写入操作日志（操作人、动作、对象、时间、原因）。
    - **界面交互规则**：重处置（删除/封号/追分）需二次确认弹窗并填写原因。
- **异常场景处理**：
    - 处置对象已被其他管理员处理：提示"该内容已处理"，刷新状态。
    - 追回积分超出用户余额：追回至余额 0，流水记录实际追回值[待补充：是否允许负余额，建议不允许]。
- **验收标准**：
    1. Given 管理员删除违规帖子，When 普通用户访问原链接，Then 提示"内容不存在或已删除"，且该帖从广场、搜索、知识库中移除。
    2. Given 管理员封禁账号 7 天，When 该账号尝试登录，Then 提示"账号已被封禁"且登录失败。
    3. Given 管理员追回某回答采纳积分 30，When 查看该用户积分明细，Then 存在"追回 -30"流水。
    4. Given 任意处置操作完成，When 查看操作日志，Then 记录完整（操作人/动作/对象/时间/原因）。

---

## 第二部分：P1 功能（V1.x 交付，部分详述）

### 功能模块：M8 AI 能力矩阵

#### 功能名称：LLM 统一网关【M8-F31｜P0 接口契约 / P1 实现】

- **功能描述**：统一封装所有 AI 场景的模型调用，提供场景路由、提示词模板、失败降级、调用日志与成本统计能力，模型可热替换。
- **用户操作流程**：（系统功能，无用户操作。P0 交付物：接口契约定义——各场景输入/输出 JSON Schema；P1 交付实现与接入真实模型。）
- **业务逻辑与交互规则**：
    - **输入/前置条件**：调用方为各 AI 功能模块（摘要/参考回答/检测/勘误/违规分级）。
    - **核心处理逻辑**：场景路由——生成类（摘要/参考回答）默认 GLM-4.5-Flash，推理类（可靠性/质量检测/勘误判定）默认 DeepSeek-V3，违规检测为安全模型+关键词前置过滤；全部调用记录日志（场景/输入摘要/输出/tokens/耗时/结果）；模型配置热替换不改业务代码。
    - **界面交互规则**：无直接界面；失败降级策略由调用方按总册 §10.4 执行。
- **异常场景处理**：模型超时/失败→按场景降级（摘要→无摘要；参考回答→不展示；检测→记录待重试）；网关整体故障→告警，不影响非 AI 主流程。
- **验收标准**：
    1. Given P0 阶段，When 评审接口契约，Then 摘要/参考回答/检测/勘误/违规分级 5 类场景的输入输出 Schema 定义完整。
    2. Given P1 实现，When 替换模型配置，Then 业务功能无需发版即切换生效。
    3. Given 任一模型调用失败，When 查看降级结果，Then 主流程（发帖/采纳）不受阻塞。

#### 功能名称：AI 摘要生成【M8-F32｜P1（P0 页面占位）】

- **功能描述**：发帖时 AI 自动生成问题摘要，供提问者确认或修改，并展示于帖子详情页供浏览者快速了解问题。
- **用户操作流程**：
    1. 提问者完成正文/图片/标题/标签填写后，点击"生成 AI 摘要"（P0 该区域显示"AI 摘要功能即将上线"占位）。
    2. 系统异步生成摘要并回填预览区（带"AI 生成"标识）。
    3. 提问者可直接采用或编辑修改后确认。
    4. 发布后摘要展示于帖子详情页顶部（带 AI 标识）。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：正文与标题已填写；调用 LLM 网关（M8-F31）。
    - **核心处理逻辑**：生成异步不阻塞发布（总册 §10.3）；摘要字数上限[待补充：建议 100 字]；用户修改内容记录 `ai_summary_edit` 埋点。
    - **界面交互规则**：生成中显示加载态；生成失败显示"摘要生成失败"并提供"跳过"与"重试"。
- **异常场景处理**：生成失败→允许无摘要发布；超时（建议 10s[待补充]）→同失败处理。
- **验收标准**：
    1. Given 合法帖子内容，When 点击生成摘要，Then 10 秒内返回带 AI 标识的摘要并可编辑。
    2. Given 摘要生成失败，When 用户选择跳过，Then 帖子正常发布且详情页不展示摘要区。
    3. Given P0 版本发帖页，When 查看 AI 摘要区，Then 仅显示占位文案，无生成入口。

#### 功能名称：AI 参考回答【M8-F33｜P1】

- **功能描述**：以官方 AI 账号在全部帖子下挂载 AI 参考回答，供用户对比参考，默认折叠且延迟展示。
- **用户操作流程**：
    1. 帖子发布后达到延迟阈值（默认 2 小时，可配），系统以官方 AI 账号生成并挂载参考回答（带固定 AI 标识）。
    2. 浏览者进入帖子详情页，AI 参考回答以折叠卡片展示于回答列表之外的区域。
    3. 点击展开查看全文，触发 `ai_ref_answer_view` 埋点。
- **业务逻辑与交互规则**：
    - **输入/前置条件**：帖子发布时间超过延迟阈值；调用 LLM 网关。
    - **核心处理逻辑**：官方 AI 账号发布、固定标识、不可被采纳、不参与回答排序与助人榜（总册 §6.6）；延迟展示期间提问者仅见真人回答；监控"只看 AI 不互动"占比超 30% 告警。
    - **界面交互规则**：折叠卡片带"AI 参考回答"醒目标识；展示位置与真人回答区视觉分离。
- **异常场景处理**：生成失败→不展示该区；帖子被删除→同步撤下。
- **验收标准**：
    1. Given 帖子发布 1 小时（阈值 2h），When 查看详情页，Then 不显示 AI 参考回答区。
    2. Given 帖子发布超过 2 小时，When 查看详情页，Then AI 参考回答以折叠态展示且带 AI 标识。
    3. Given AI 参考回答存在，When 提问者尝试采纳，Then 无采纳入口（AI 回答不可被采纳）。
    4. Given AI 参考回答，When 查看助人榜，Then 官方 AI 账号不出现在任何榜单。

#### 功能名称：回答可靠性检测【M8-F34｜P1】

- **功能描述**：提问者采纳回答时，AI 对比检测回答的质量与正确性，结果辅助展示。
- **用户操作流程**：（系统自动）采纳操作触发检测，检测结果以辅助信息形式展示[待补充：展示形式——如"AI 检测：该回答与 AI 理解一致性较高/存疑"标签，待评审]。
- **业务逻辑与交互规则**：检测异步执行，不阻塞采纳流程；检测为辅助参考，不改变采纳结果与积分发放。
- **异常场景处理**：检测失败→不展示检测结果，采纳流程正常。
- **验收标准**：
    1. Given 采纳触发检测，When 检测完成，Then 结果以[待补充：确定的展示形式]呈现且不影响积分发放时效。

#### 功能名称：回答质量检测与积分追回【M8-F35｜P1】

- **功能描述**：对被采纳回答执行"先发后审"的质量检测，低质量高频用户被追回采纳积分。
- **用户操作流程**：（系统自动）采纳积分即时发放→后台异步质量检测→命中低质量高频规则时追回积分并站内通知。
- **业务逻辑与交互规则**：
    - **核心处理逻辑**：判定标准[待补充：低质量定义（如复制粘贴/AI 灌水/与问题无关）与"高频"阈值，V1.x 开发前评审]；追回金额=该回答全部采纳积分（总册 §6.3）；追回写入流水并通知用户，通知含申诉入口。
    - **界面交互规则**：通知文案示例："你的回答经质量检测未达标，已追回 30 积分。如有异议可申诉。"
- **异常场景处理**：检测失败→记录待重试；申诉→进入管理员队列（M7-F29）。
- **验收标准**：
    1. Given 用户单周 5 次采纳被判定低质量高频（示例阈值），When 检测完成，Then 5 笔采纳积分全部追回且用户收到通知。
    2. Given 追回执行，When 查看积分明细，Then 存在对应"追回"流水。

### 功能模块：M7 治理与合规（P1 部分）

#### 功能名称：AI 违规检测与分级处置【M7-F27｜P1】

- **功能描述**：AI 对内容（含被举报内容）进行违规检测，按置信度分级处置：极高→自动透明化，高→推送人工，低→仅记录。
- **用户操作流程**：
    1. 用户提交举报（M7-F25）或 AI 主动巡检触发检测。
    2. AI 复检并分级：极高置信违规→自动透明化（内容不删除，但广场/搜索不展示，直达链接仍可访问）；高→推送管理员队列；低→仅记录。
    3. 被透明化内容作者收到通知与申诉入口。
- **业务逻辑与交互规则**：
    - **核心处理逻辑**：AI 仅可执行透明化（非删除类处置），删除与封号必须人工（总册 §6.6）；分级阈值[待补充：V1.x 开发前确定]；关键词词库前置过滤降低调用量。
    - **界面交互规则**：被透明化内容对普通用户在广场/搜索不可见；作者视角显示"该内容已被限流，申诉"。
- **异常场景处理**：检测失败→按 P0 路径进入人工队列兜底。
- **验收标准**：
    1. Given 内容被判定极高置信违规，When 自动处置完成，Then 广场与搜索不展示该内容，但通过直达链接可访问且带申诉入口。
    2. Given 内容被判定高置信，When 处置完成，Then 内容正常展示且管理员队列出现待处理记录。
    3. Given 申诉成功，When 管理员恢复内容，Then 内容重新进入广场与搜索。

#### 功能名称：纠错反馈与 AI 勘误【M7-F28｜P1】

- **功能描述**：用户对知识库内容提交纠错反馈，AI 判定成立后原答案保留并新增"AI 勘误"标签与 AI 勘误版本。
- **用户操作流程**：
    1. 用户在已解决帖的采纳回答上点击"纠错反馈"。
    2. 填写纠错说明（错在哪、正确理解）并提交。
    3. AI 判定反馈正确性：成立→该回答新增"AI 勘误"标签，下方展示 AI 勘误版本，原答案保留展示；不成立→仅记录，不展示。
    4. 反馈者与回答者收到结果通知。
- **业务逻辑与交互规则**：
    - **核心处理逻辑**：原采纳答案永不被 AI 下架（总册 §6.5）；AI 勘误版本带显式 AI 标识；判定阈值[待补充]。
    - **界面交互规则**："AI 勘误"标签醒目展示于原答案旁；勘误版本折叠展开查看。
- **异常场景处理**：判定失败→记录待重试，反馈者收到"处理中"状态。
- **验收标准**：
    1. Given 纠错反馈被 AI 判定成立，When 查看该回答，Then 原答案保留 + "AI 勘误"标签 + AI 勘误版本三要素齐全。
    2. Given 纠错被判定不成立，When 查看该回答，Then 回答展示无变化。

#### 功能名称：管理员完整后台【M7-F29｜P1】

- **功能描述**：在 P0 基础处置之上，提供带 AI 分级的举报队列、处置面板、数据看板与完整操作日志。
- **用户操作流程**：
    1. 管理员登录后台，查看按 AI 分级排序的举报队列（高置信优先）。
    2. 处置面板支持：删除/封号/追分/透明化/恢复（申诉处理）。
    3. 数据看板查看违规量、误判申诉率、勘误触发率等风控指标（总册 §9）。
    4. AI 提示操作与人工直接操作双通道并存。
- **业务逻辑与交互规则**：核心规则同 M7-F26，扩展透明化与恢复权限；全部操作留痕。
- **异常场景处理**：[待补充：V1.x 开发前详述]
- **验收标准**：[待补充：V1.x 开发前按看板与队列功能逐条补充]

### 功能模块：M5 激励体系（P1 部分）

#### 功能名称：日常任务体系【M5-F20｜P1】

- **功能描述**：用户完成每日任务（发首帖/交首答/有效浏览 3 帖）获得积分，任务中心展示任务状态。
- **用户操作流程**：
    1. 用户进入"任务中心"页查看今日任务与奖励。
    2. 完成任务（当日发首帖 +5 / 交首答 +10 / 有效浏览 3 帖 +5）。
    3. 任务达标自动发放积分，任务状态更新为"已完成"。
- **业务逻辑与交互规则**：任务合计日封顶 20（总册 §6.3）；"有效浏览"定义[待补充：停留时长阈值，防刷新刷量]；任务与登录积分共同计入日产出封顶 100。
- **异常场景处理**：发放失败自动重试；跨日任务进度清零（时区以 Asia/Shanghai 为准）。
- **验收标准**：
    1. Given 用户当日完成任务全部三项，When 最后一项达成，Then 获得 20 积分且任务均显示已完成。
    2. Given 用户当日登录 5 分+任务 20 分+采纳 75 分已达 100 封顶，When 再次被采纳，Then 采纳积分不发放、感谢值正常。

### 功能模块：M4 搜索与知识库（P1 部分）

#### 功能名称：AI 相似问答匹配【M4-F16｜P1】

- **功能描述**：在搜索侧引入 AI 提升问答匹配的相关性（辅助 P0 的标题模糊+标签筛选）。
- **用户操作流程**：（搜索体验增强，无独立操作；用户搜索时系统用 AI 对候选结果做相关性重排或扩展召回。）
- **业务逻辑与交互规则**：实现方式[待补充：V1.x 评审确定——向量召回或 LLM 重排]；降级路径：AI 失败时回退 P0 的标题模糊+标签匹配，保证搜索可用。
- **异常场景处理**：AI 超时→回退 P0 匹配，不阻塞搜索响应（≤1s 约束见总册 §10.3）。
- **验收标准**：
    1. Given AI 匹配服务故障，When 用户搜索，Then 搜索功能可用（回退 P0 逻辑）且响应 ≤1s。

### 功能模块：M1 账号与个人中心（P1 部分）

#### 功能名称：本校/本专业过滤【M1-F4｜P1】

- **功能描述**：广场新增"本校""本专业"Tab，按用户资料中的学校/专业过滤帖子。
- **用户操作流程**：
    1. 用户在广场切换到"本校"或"本专业"Tab。
    2. 列表仅展示发布者资料匹配的帖子。
    3. 未填写学校/专业的用户看到引导页："设置你的学校和专业，查看同校动态"→跳转资料编辑。
- **业务逻辑与交互规则**：过滤依据为发帖者自填的学校/专业字段（无认证，允许不匹配的真实情况存在）；Tab 内排序逻辑与"推荐/最新"一致。
- **异常场景处理**：匹配结果为空显示空态"本校还没有相关帖子，去提第一个问吧"。
- **验收标准**：
    1. Given 用户资料学校为"XX 大学"，When 切换本校 Tab，Then 列表全部帖子发布者学校为"XX 大学"。
    2. Given 未填写专业的用户，When 切换本专业 Tab，Then 显示资料引导页而非空列表。

### 功能模块：M5 激励体系（商城接口）

#### 功能名称：积分商城接口【M5-F21｜P1】

- **功能描述**：预留商品模型与积分兑换接口，不做交易闭环与商品实际上架。
- **用户操作流程**：（P1 无用户界面或仅占位页；交付物为接口契约：商品模型（类型/价格/库存/上下架状态）、兑换接口（校验余额→扣分→发放记录）、"商城兑换"积分流水来源枚举。）
- **业务逻辑与交互规则**：兑换扣分走积分账户事务（M5-F17），保证不透支；商品类型枚举预留：虚拟（平台装饰/气泡/特效）、实物（最后版本）。
- **异常场景处理**：[待补充：V1.x 接口评审时详述]
- **验收标准**：
    1. Given P1 交付，When 开发评审接口契约，Then 商品模型与兑换接口定义完整且与积分流水打通（沙箱验证扣分与流水一致）。

---

## 第三部分：P2 功能（V2.0 交付，占位）

### 功能模块：M9 社交与个性化

#### 功能名称：私聊【M9-F36｜P2】

- **功能描述**：用户之间的一对一私信沟通。
- **用户操作流程**：[待补充：V2.0 开发前详述]
- **业务逻辑与交互规则**：[待补充：消息存储、已读未读、防骚扰策略（陌生人私信开关）等，V2.0 前评审]
- **异常场景处理**：[待补充]
- **验收标准**：[待补充]

#### 功能名称：群聊【M9-F37｜P2】

- **功能描述**：支持 100 人以内的群组聊天（如班级学习群、课程讨论群）。
- **用户操作流程**：[待补充：V2.0 开发前详述]
- **业务逻辑与交互规则**：**群人数硬上限 100 人**（Out-of-Scope 约束）；建群/入群/踢人权限模型[待补充]。
- **异常场景处理**：[待补充]
- **验收标准**：
    1. Given 群成员已达 100 人，When 第 101 人申请入群，Then 被拒绝并提示"群成员已达上限"。

#### 功能名称：个性化装饰【M1-F5｜P2】

- **功能描述**：用户购买并配置个人主页装饰、背景图、聊天气泡、评论点赞特效等个性化元素（参考 QQ 个性化体系）。
- **用户操作流程**：[待补充：V2.0 开发前详述]
- **业务逻辑与交互规则**：装饰商品来自积分商城（M5-F22），购买后绑定账号并可在装扮页启用/停用；聊天气泡依赖私聊功能上线。
- **异常场景处理**：[待补充]
- **验收标准**：[待补充]

#### 功能名称：商城虚拟商品兑换【M5-F22｜P2】

- **功能描述**：用户使用积分兑换平台装饰、聊天气泡、点赞特效等虚拟商品。
- **用户操作流程**：[待补充：V2.0 开发前详述]
- **业务逻辑与交互规则**：兑换走 M5-F21 接口；虚拟商品发放即时生效。
- **异常场景处理**：[待补充]
- **验收标准**：[待补充]

#### 功能名称：商城实物兑换【M5-F23｜P2 最后版本】

- **功能描述**：用户使用积分兑换文创实物商品。
- **用户操作流程**：[待补充]
- **业务逻辑与交互规则**：需供应链、收货地址、兑换门槛（防积分工作室套现）[待补充]。
- **异常场景处理**：[待补充]
- **验收标准**：[待补充]

#### 功能名称：管理员晋升通道【M7-F30｜P2】

- **功能描述**：为长期高感谢值用户提供管理员账号授权接口。
- **用户操作流程**：[待补充]
- **业务逻辑与交互规则**：晋升标准（感谢值阈值+历史违规记录审查）[待补充]；授予的权限范围应小于正式运营管理员[待补充]。
- **异常场景处理**：[待补充]
- **验收标准**：[待补充]
