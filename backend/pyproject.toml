[project]
name = "changxue-backend"
version = "0.1.0"
description = "畅学社区后端（FastAPI 单体）"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "PyJWT>=2.8",
    "bcrypt>=4.1",
    "apscheduler>=3.10",
    "httpx>=0.27",
    "python-multipart",
    "pillow",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
mysql = ["pymysql>=1.1"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
